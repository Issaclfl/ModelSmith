# 实测循环（赛前打磨用）

纪律：**仓库是唯一真源**。每轮测试后新坑当轮写回 references/PITFALLS.md（或
SKILL.md 硬约束），再同步，再测。功力留在对话里 = 白测。

## 每轮流程

1. 改技能包（或确认已是想测的版本）
2. `python sync_to_local.py` 同步到本机安装目录（或双击 `同步到本机.bat`）
3. **新开一个干净会话**（上下文零污染，否则测的是对话功力不是技能）
4. 用下面的指令模板发起测试，按"观察点清单"逐项盯
5. 发现新坑 → 当轮写回 → 再同步
6. 在 TESTLOG.md 追加一节

## 测试指令模板

```
用 modelsmith 技能求解以下题目：<题目描述 / PDF·附件路径>
数据在：<数据目录路径>
要求：完整流程（建模→求解→验证→论文 PDF），过程产出保存到 <输出目录>
```

## 观察点清单

- [ ] 技能是否被触发（agent 是按 SKILL.md 工作流走，还是自由发挥）
- [ ] 动手前是否读 PITFALLS / DISCIPLINE
- [ ] 题型路由是否选对建模路线与范例
- [ ] 硬约束是否真的执行（双角度一致性、谐波判据、偷懒检测等）
- [ ] 求解代码含 `_selftest()` 且通过；审计门禁（scripts/audit）跑了没被绕过
- [ ] 论文数值抽查 3 个可溯源（代码输出 ↔ 论文）
- [ ] 审计阈值松紧：产出论文过 `audit_runner.py`，该过的过、不该卡的都不卡（防漂移）
- [ ] 全流程耗时与卡点

## 一致性测试（定期做，防指令歧义）

同一道题独立跑 2-3 次（新会话、零上下文），**只比对结构决策**：题型路由、方法选择、
纪律执行（确认单/沙箱/多种子/审计是否被触发）、核心数值量级。两次不一致的地方 =
SKILL.md 存在解释空间，定点修文档后再测。
注意：文风与措辞多样性是反 AI 检测的刻意设计（WRITING_STYLE 第六节），
**不纳入一致性比对**。

## 难度阶梯（建议顺序）

1. **2023B 测线**（解析几何，范例成熟）——验证管线通不通
2. **2023A 镜场**（几何优化，范例在）——验证优化类
3. **2025B 干涉光谱**（新题，坑最多）——验证硬约束与 FFT 交叉验证
4. **一道没做过的新题**——验证零范例泛化（真考场景）

## 开源前检查

- [ ] TESTLOG.md 清空或移出仓库
- [ ] README 定位改写为"给 AI agent 装数模外挂" + 安装三步（clone → `python sync_to_local.py` → 开讲）
- [ ] `python scripts/audit/selftest.py` 全绿
- [ ] examples 里无本机绝对路径残留
