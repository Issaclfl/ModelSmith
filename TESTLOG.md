# 测试日志（教训索引）

> 2026-09 仓库公开起，涉及具体赛题与成绩的测试记录移出仓库（内部留存），
> 这里只保留可公开的**经验教训条目**——每一条的完整规则都已写进
> references/（PITFALLS / DISCIPLINE / WRITING_STYLE），此处作索引。

<!-- 每轮模板：
## YYYY-MM-DD · 主题 · 第N轮
- 新坑：
- 写回：
-->

## 2026-09-02 · 外部综合评审 · 反馈写回
- 新坑：无差异意识的范例复制 = 记忆型行为，与引擎已具备的"突破模板"能力自相矛盾；
  知识库/阈值/指令歧义三类漂移风险需要机制兜底
- 写回：MODEL_PLAYBOOK **三差异规则**（列不出 3 个物理差异不配用范例）+
  SKILL.md 挂钩 + DISCIPLINE 七 晋升规则（同一坑 ≥2 次复现 → PITFALLS）+
  TESTING.md 一致性测试（同题多跑只比结构决策）

## 2026-09-02 · 摘要专项评审 · 反馈写回
- 新坑：**报账体**——只有结果没有理由；约束写成黑话（无数值）；排序决策不给淘汰逻辑。
  根因在写作侧不在建模侧
- 写回：WRITING_STYLE 第三节整体重写（黄金三句式 / 约束句模板 / 三条筛选规则 /
  现场改写示范 / 硬性自查 7 项）+ SKILL.md 第4步挂钩 + PITFALLS 新增报账体坑

## 2026-09-02 · 求新能力与结论稳健性评审 · 反馈写回
- 新坑：工具箱全是教科书标准件、缺"有纪律的求新行为"；**披露残差 ≠ 看穿残差**
  对核心结论的杀伤半径
- 写回：DISCIPLINE 三.6-3.8（问题重构 / 专属判据 / 跨域扫描）+ 五.7
  **边际-残差核对**（优势边际必须大于已披露残差带）+ PITFALLS 新增"结论稳健性"节 +
  WRITING_STYLE 五.11（优劣结论边际声明）+ SKILL.md 第1/2步挂钩

## 2026-09-02 · 工程可靠性
- 新坑：CI 依赖 actions/setup-python 的 `cache: pip` 却无 requirements.txt，
  action 直接失败（测试根本没跑）；非 raw 字符串里的 `\{` 转义在 Py3.12 出 SyntaxWarning；
  **范例脚本用 Py3.12 才合法的 f-string 同引号嵌套写法（`f'...{d["k"]}...'`），
  Py3.10 直接 SyntaxError**——agent 在宿主最新版 Python 上写码，兼容性要靠 CI 双版本矩阵兜底
- 写回：requirements.txt + 工作流改用依赖清单 + 转义修复 + 范例引号风格修正
  （此条同时是对"每次修改后必须本地复现 CI"纪律的执行记录）
